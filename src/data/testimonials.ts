import type { Testimonial } from '@/types/common'

/**
 * Placeholder — no testimonial copy was supplied in any of the design
 * documents. Leave empty (rather than fabricating quotes) until real
 * testimonials are provided; consuming components should handle an empty list.
 */
export const testimonials: Testimonial[] = []
